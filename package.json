{
  "name": "medtopix",
  "private": true,
  "type": "module",
  "scripts": {
    "db:migrate": "node --env-file=.env.local db/migrate.mjs",
    "test": "node --env-file=.env.local tests/roles.mjs && node --env-file=.env.local tests/alarmas.mjs && node --env-file=.env.local tests/fhir.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0",
    "web-push": "^3.6.7"
  }
}
